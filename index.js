function palindrome(str) {
    let letters = [];
    let regix = ReqExp('', 'g');

// remove special characters, while spaces and punctuation
str = str.replace(regix, '');

      return true;
    }
    
    
    
    palindrome("eye");